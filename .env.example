# Copy to .env and customize: cp .env.example .env

POSTGRES_PASSWORD=changeme

# Local Python/dbt connecting to Docker Postgres (host port 5433)
POSTGRES_HOST=localhost
POSTGRES_PORT=5433
POSTGRES_USER=postgres
POSTGRES_DB=autoanalyst

# Optional: full LLM investigation reports (see src/agent/README.md)
# OPENAI_API_KEY=
